{
  "name": "siema-tech-task",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev,start:dev --stream --parallel --no-bail",
    "build": "lerna run build --no-bail",
    "clean": "lerna run clean --no-bail"
  },
  "devDependencies": {
    "lerna": "^8.1.5",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "punycode": "^2.3.1"
  }
}
